#include <stdio.h>
#include <string.h>

int main(void)
{
    /*
    int ctr = 0;
    
    while (ctr < 5)
    {
        printf("Counter is at %d.\n", ++ctr);
    }
    
    while (ctr > 1)
    {
        printf("Counter is at %d.\n", --ctr);
    }
*/
/*
float num1, num2, result;
char choice;

do {

    printf("Enter your first number to multiply: ");
    scanf (" %f", &num1);

    printf("Enter your second number to multiply: ");
    scanf (" %f", &num2);

    result = num1 * num2;

    printf("%.2f times %.2f equals %.2f\n\n", num1, num2, result);

    printf("Do you want to enter another pair of numbers ");
    printf("to multiply (Y/N): ");
    scanf(" %c", &choice);

    // If the user enters a lowercase n, this if statement will convert it to an N

       if (choice == 'n')
        {
          choice = 'N';
        }

} while (choice != 'N' && choice != 'n');

*/
/*
int cDown;
for (cDown = 10; cDown > 0; cDown--)
{
    printf("%d.\n", cDown);
}
printf("Blast off!\n");
*/
/*
int inner;
int outer;
for (outer = 1; outer <= 3; outer++)
{
    for (inner = 1; inner <=5; inner++)
    {
        printf("%d ", inner);
    }
    printf("\n ");
}
*/
/*
int fags = 0;
do {
    printf("What color is your favorite color\n");
    fags++;
}
while(fags < 10);
*/
/*
for(int sun = 0; sun < 10; sun++ )
{
    printf("There are going to be around 10 new suns\n");
}
*/
/*
int sun = 0;
while(sun < 10)
{
    printf("There are going to be around 10 new suns\n");  
    sun++;  
}
*/

 

}