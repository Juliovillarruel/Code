#include <stdio.h>
#include <string.h>

int main(void)
{
    /*
    int grade = 94;
    int classMissed = 2;
    int numActs = 1;
    int sports = 5;

    if ((grade > 93) && (classMissed <= 3) && ((numActs >= 3) || (sports >= 2)) )
    {
        printf("The student will get a reward.\n");
    }
*/

    int radiator = 49; 
    int engine = 95; 
    int brakes = 43;
    int exhaust_pipe = 51;
/*
    if ( (radiator > 60) && (engine > 50) && ((exhaust_pipe > 50) || (brakes > 50)) )
    {
        printf("It looks like it worked\n");
    }
    */

/*
   (radiator > 50) ? (radiator += 1): (brakes += 1);
   printf("The result is %d", radiator);
   printf("The result is %d", brakes);
*/

int love = 30; 
int hate = 50;
int mediumship = 50; 

(mediumship < 40) ? printf("Mediumship value is greater than 40"): printf("I guess mediumship is not above 40");


}