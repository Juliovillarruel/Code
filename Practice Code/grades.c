#include <stdio.h>
#include <string.h>

int main(void)
{

    // Data Set
    int julio = 85;
    int diana = 90;
    int manny = 87;
    int angela = 95;
    int steven = 96;

/*
    for (int i = 0; i < 10; i++)
    {
        // printf("The system has deteced a value of 5", i);
        if (i == 5)
        {
            printf("We have found a 5\n");
        }
    }
    */  
   /*
    int i = 0; 
    int value;
    int newValue;
    int oldValue = 360;
    do 
    {
        printf("Give me a number 1-4");
        scanf(" %d", &i);

        printf("Give me a value");
        scanf(" %d", &value);

        newValue = oldValue / value;
        printf("This is the new value: %d\n", newValue);
    }
    while (i == 4);
    
    */

int hello = (2 * 5) - 2 * 100;
printf("Hello, this is the answer %d", hello);


}